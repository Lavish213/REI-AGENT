    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))B    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))^    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))*    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))!    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))?    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))\    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))*    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))$    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))U    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))—    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))J    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))q    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))B    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))W    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))?    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))+    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))1    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))%    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))5    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))!    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))6    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))!    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))6    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))U    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))I    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))C    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))L    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))U    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))P    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))H    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))A    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))G    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))O    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))B    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))Y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))!    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))4    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))S    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))W    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))j    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))5    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))K    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))R    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))D    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))!    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))1    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))T    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))[    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))]    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))5    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))|    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))-    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))>    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))N    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))y    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))5    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))m    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))0    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))h    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))v    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))'    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))F    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))u    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))E    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))x    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))p    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)):    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)).    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))w    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))g    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))n    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))b    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))c    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))k    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))f    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))o    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))=    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)){    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))}    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))"    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))l    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))a    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))_    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))i    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))d    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)),    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))     def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))s    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))t    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))r    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))(    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))e    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e)))    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))
    def _run_orchestrator(self) -> None:
        try:
            decision = self._runtime_orchestrator.decide(self._ctx)
            if decision.objective_override:
                self._ctx.objective = decision.objective_override
            if decision.inject_instruction:
                self._ctx.runtime_instruction = decision.inject_instruction
            if decision.end_call:
                logger.info("orchestrator end_call reason={}", decision.end_reason)
                self._ctx.call_should_end = True
        except Exception as e:
            logger.warning("orchestrator failed error={}", str(e))